services:
  - type: web
    name: vex-app
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: ENV
        value: production
