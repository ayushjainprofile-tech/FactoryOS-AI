{
  "name": "backend",
  "type": "app_sail",
  "stack": "python39",
  "env": {
    "PYTHON_VERSION": "3.9"
  },
  "build": {
    "command": "pip install -r requirements.txt"
  },
  "run": {
    "command": "uvicorn src.main:app --host 0.0.0.0 --port $X_ZOHO_CATALYST_LISTEN_PORT"
  }
}
